{% if user.isLoggedIn %}
    <span>Welcome {{ user.name }}</span>
    <button class="btn btn-primary btn-sm" id="login-btn" data-task="logout">Log Out</button>
{% else %}
    <button class="btn btn-primary btn-sm" id="login-btn" data-task="login">Log In</button>
{% endif %}